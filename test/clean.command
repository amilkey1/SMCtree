#!/bin/bash

DIR="$( cd "$( dirname "$0" )" && pwd )"
cd $DIR

rm -f sim.tre
rm -f sim.nex
rm -f mltree.tre
rm -f pauplog.txt
rm -f sim-paup.nex
rm -f pldist.txt
rm -f rfdist.txt
rm -f trees.trees
rm -f params.log
rm -f unique_trees.trees
